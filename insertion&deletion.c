#include<stdio.h>
#include<stdlib.h>
struct Node{
    int data;
    struct Node *ptr;
};
typedef struct Node node;
void satck(node *start){
// while(start->ptr!=NULL){
    // scanf("%d",&start)
// }
}
int main()
{
node *head;
node *first;
// head=(node *)malloc(sizeof(node));
printf("The size is %d",sizeof(struct Node));
// node *third;
// node *fourth;
// values(head);
}